# Domain Design Plan

Use this before scaffolding a new DDD domain. Forces you to define boundaries, entities, and responsibilities before writing any code.

---

## Prompt

---

## When to use
- Before calling `/scaffold <domain>`
- When defining a new bounded context
- When refactoring a large module into a proper domain
