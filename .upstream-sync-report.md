# TradingAgents 上游同步方案（v2 - 精确剥离版）

**生成时间**: 2026-07-09 12:35 GMT+8
**上游**: TauricResearch/TradingAgents v0.3.0 → v0.3.1（merge-base `a5cb7cb` → head `01477f9`，22 个新提交）
**本地**: david188888/TradingAgents main（HEAD `45e3ca6`）
**策略**: 取架构改进，弃功能扩张；同文件内精确剥离

---

## 一、第一性原理：决策依据

### 用户的真实需求

用户是独立使用者，自己配置模型和 API。因此：

- **要的是上游的架构改进**：图构建重构、vendor 路由层、符号标准化、结构化输出、分析执行计划、工具函数集、模型目录刷新、CLI 重构。这些让 fork 的工程质量对齐上游。
- **不要的是上游的功能扩张**：为团队/企业用户加的托管 provider 和数据源。用户自己配模型，不需要 Bedrock/KIMI/GROQ/Mistral/NVIDIA；不需要 Polymarket 预测市场。

### 关键事实：架构与扩张在上游是捆绑的

上游 v0.3.0 把"provider registry 架构"和"新增 provider 实体"写在同一批文件里。因此不能整体 take/adapt，必须**在同文件内剥离**：保留架构骨架，删除不要的 provider/数据源条目。

CHANGELOG 印证这一点（v0.3.0 Added）：
> "Provider registry. OpenAI-compatible providers register as a single spec ... Adds NVIDIA NIM, Kimi, Groq, Mistral, and a native Amazon Bedrock client."

架构（registry）和实体（NIM/Kimi/...）是分开描述的 → 剥离可行。

### 最终决策清单

| 类别 | 决策 |
|------|------|
| Bedrock（Amazon） | ❌ 不要 |
| KIMI（Moonshot） | ❌ 不要 |
| GROQ | ❌ 不要 |
| Mistral | ❌ 不要 |
| NVIDIA NIM | ❌ 不要 |
| openai_compatible（通用本地端点） | ❌ 不要（只用预置 provider + Ollama） |
| Polymarket（预测市场数据源） | ❌ 不要 |
| FRED（宏观经济数据源） | ✅ 保留（对股票/A 股分析有价值） |
| 身份解析 | 🔧 混合：A 股用本地三级（tushare/akshare/yfinance），非 A 股用上游 yfinance |
| 所有架构改进 | ✅ 保留 |
| 本地独有功能（Evidence Steward/china_data/tavily/中文化） | 🔒 保护 |

---

## 二、保留的架构改进（完整清单）

以下是从上游 v0.3.0/v0.3.1 保留的核心改进，按 CHANGELOG 对照确认：

### 来自 v0.3.0

| 改进 | 涉及文件 | 状态 |
|------|---------|------|
| Provider registry 架构（ProviderSpec 注册表） | `openai_client.py`, `factory.py` | ✅ 保留架构，删不要的 provider 条目 |
| Verified data-access contract（符号标准化 + 严格 vendor chain + VendorError + look-ahead + stale OHLCV） | `symbol_utils.py`, `interface.py`, `errors.py`, `y_finance.py`, `yfinance_news.py` | ✅ 完整保留 |
| Config precedence（env/flag 优先于交互默认，非法 boolean 报错） | `default_config.py`, `cli/main.py` | ✅ 保留 |
| Model catalog refresh（Claude 5/GPT-5.5/DeepSeek V4/Qwen 3.7/MiniMax M3/Gemini 3.5/Grok 4.3） | `model_catalog.py` | ✅ 保留刷新，删不要的 provider 条目 |
| Optional vendors degrade（可选数据源失败不中断） | `interface.py` `route_to_vendor` + VendorError 体系 | ✅ 等价保留（上游 v0.3.0 重构已用 VendorError chain 替代旧 `OPTIONAL_CATEGORIES`，macro_data 失败时优雅降级） |
| Analyst prompts lead with current date | 各 analyst prompt | ✅ 保留 |
| Programmatic report output（`save_reports()`） | `reporting.py`, `trading_graph.py` | ✅ 保留 |
| Env-configurable reasoning depth | `default_config.py`, `cli/main.py` | ✅ 保留 |
| Instrument identity + verified market snapshot（防幻觉） | `agent_utils.py`, `market_data_validation_tools.py` | ✅ 保留 |
| Reddit RSS-first / StockTwits / Alpha Vantage 健壮性 | `reddit.py`, `stocktwits.py`, `alpha_vantage*.py` | ✅ 保留 |
| Structured output hardening（本地服务器 tool_choice / thinking model fallback / null-ish float） | `openai_client.py`, `structured.py`, `schemas.py` | ✅ 保留 |
| FRED 宏观指标 | `fred.py`, `macro_data_tools.py` | ✅ 保留 |
| CI gate（GitHub Actions） | `.github/workflows/ci.yml` | ✅ 保留 |
| ruff 全仓库 lint | 多文件 | ✅ 保留 |

### 来自 v0.3.1

| 改进 | 涉及文件 | 状态 |
|------|---------|------|
| Alpha Vantage look-ahead filter 实际生效 | `alpha_vantage_fundamentals.py` | ✅ 保留 |
| News analyst prompt 对齐 tool 签名 | `news_analyst.py` | ✅ 保留（剥离 prediction_markets 部分） |
| debate/risk router 完整 path_map（防崩溃） | `setup.py` | ✅ 保留 |
| Checkpoint 图形态感知（thread_id 折入 signature） | `checkpointer.py`, `trading_graph.py` | ✅ 保留 |
| Crypto sentiment 源映射（StockTwits/Reddit） | `stocktwits.py`, `reddit.py` | ✅ 保留 |
| llm_max_retries 配置 | `default_config.py`, `trading_graph.py` | ✅ 保留 |
| Claude Sonnet 5 / Fable 5 模型 | `model_catalog.py`, `anthropic_client.py` | ✅ 保留 |

---

## 三、精确剥离清单（按文件）

这是施工的核心。每个文件列出**保留什么、删除什么**。

### 3.1 LLM Provider 层

#### `tradingagents/llm_clients/openai_client.py`
- ✅ **保留**：`ProviderSpec` dataclass 架构、`OPENAI_COMPATIBLE_PROVIDERS` 注册表机制、`NormalizedChatOpenAI`、`DeepSeekChatOpenAI`、`MinimaxChatOpenAI`（含 extra_body 修复 #826）、`_supports_reasoning_effort()`、`is_openai_compatible()`
- ✅ **保留注册表条目**：openai, xai, deepseek, qwen, qwen-cn, glm, glm-cn, minimax, minimax-cn, openrouter, ollama
- ❌ **删除注册表条目**：mistral, kimi, groq, nvidia, openai_compatible
- ❌ **删除类**：`LocalCompatibleChatOpenAI`（仅 openai_compatible 使用）
- ⚠️ **注意**：删除 openai_compatible 后，`is_openai_compatible("openai_compatible")` 返回 False，factory.py 会抛 `ValueError: Unsupported LLM provider` —— 这是正确的拒绝行为，无悬空引用

#### `tradingagents/llm_clients/factory.py`
- ✅ **保留**：anthropic/google/azure 分支、`is_openai_compatible()` 判断、OpenAIClient 路由
- ❌ **删除**：`if provider_lower == "bedrock"` 整个分支（第 46-48 行）

#### `tradingagents/llm_clients/bedrock_client.py`
- ❌ **不引入**（整个文件不要）

#### `tradingagents/llm_clients/api_key_env.py`
- ❌ **删除**：`"bedrock": None` 条目（第 20 行）

#### `tradingagents/llm_clients/model_catalog.py`
- ✅ **保留刷新的模型**：openai(GPT-5.5)、anthropic(Sonnet 5/Fable 5/Opus 4.8)、google(Gemini 3.5)、xai(Grok 4.3)、deepseek(V4)、qwen(3.7)、glm(5.2)、minimax(M3)、openrouter
- ❌ **删除条目**：mistral, kimi, groq, nvidia, bedrock, openai_compatible
- ✅ **保留** `_CUSTOM_ONLY` 辅助字典（openrouter 仍用它）

### 3.2 CLI 层

#### `cli/utils.py`
- ✅ **保留 provider 表条目**：OpenAI, Google, Anthropic, xAI, DeepSeek, Qwen, GLM, MiniMax, OpenRouter, Azure, Ollama
- ❌ **删除 provider 表条目**（6 行）：Mistral, Kimi (Moonshot), Groq, NVIDIA NIM, Amazon Bedrock, OpenAI-compatible
- ⚠️ **检查**：第 207 行附近的 provider 偏好列表若提及 mistralai/nvidia 需同步清理

#### `cli/main.py`
- ✅ **保留**：显式 import、`thinking_value_or_prompt()`、asset_type 检测、analyst wall time tracking、env-override 交互跳过、ticker 提示更新
- 🔧 **手动 port 本地自定义**：中文本地化界面、JSON 配置文件加载、数据进度事件系统
- ❌ **删除**：若引用 openai_compatible 的交互逻辑需清理

### 3.3 数据流层

#### `tradingagents/dataflows/interface.py`
- ✅ **保留**：`route_to_vendor()` 严格 chain 重构、VendorError 分类处理（替代旧 `OPTIONAL_CATEGORIES`，macro_data 失败时优雅降级）、`TOOLS_CATEGORIES`、`VENDOR_METHODS`
- ✅ **保留 vendor**：yfinance, fred, alpha_vantage + 本地 china_data/tavily
- ❌ **删除**：`from .polymarket import ...`、`VENDOR_LIST` 中 "polymarket"、`TOOLS_CATEGORIES["prediction_markets"]`、`VENDOR_METHODS["get_prediction_markets"]`
- ⚠️ **调整**：上游 v0.3.0 重构已移除 `OPTIONAL_CATEGORIES` 机制（代码中无此符号），改由 `route_to_vendor` 的 VendorError chain 实现等价降级（macro_data 失败不中断）；`TOOLS_CATEGORIES` 中删除 prediction_markets
- 🔧 **手动添加**：本地 china_data(tushare/akshare)、tavily_news 到 VENDOR_METHODS

#### `tradingagents/dataflows/polymarket.py`
- ❌ **不引入**（整个文件不要）

#### `tradingagents/dataflows/fred.py`
- ✅ **完整引入**（237 行，纯实现，无额外依赖）

#### `tradingagents/dataflows/symbol_utils.py`
- ✅ **完全采用上游版本**（143 行）。上游实现更成熟：forex 集合、crypto_base 提取、is_yahoo_safe、NoMarketDataError re-export
- 🔧 **追加**：本地若有 A 股特殊符号映射，追加到 `_ALIASES` 表

#### `tradingagents/dataflows/errors.py`
- ✅ **完整引入**（VendorError 体系：NoMarketDataError/VendorNotConfiguredError/VendorRateLimitError）

### 3.4 Agent 工具层

#### `tradingagents/agents/utils/agent_utils.py`
- ✅ **保留**：`resolve_instrument_identity()`（LRU cache，作为非 A 股路径）、`build_instrument_context()` 新签名（asset_type + identity）、`get_instrument_context_from_state()`、`__all__` 导出
- 🔧 **身份解析策略**：A 股不走此函数（走本地 `resolve_canonical_company_profile`），非 A 股走此函数。分支在 `trading_graph.py` 的 `resolve_instrument_context()` 内实现（见 3.6 与第四节 4.1）
- ✅ **保留导入**：`get_macro_indicators`、`get_verified_market_snapshot`
- ❌ **删除导入**：`get_prediction_markets`（第 24 行）
- ❌ **删除 `__all__` 条目**：`"get_prediction_markets"`

#### `tradingagents/agents/utils/macro_data_tools.py`
- ✅ **完整引入**（FRED 宏观数据工具）

#### `tradingagents/agents/utils/market_data_validation_tools.py`
- ✅ **完整引入**（verified market snapshot，防数值幻觉）

#### `tradingagents/agents/utils/prediction_markets_tools.py`
- ❌ **不引入**（整个文件不要）

### 3.5 Analyst 层

#### `tradingagents/agents/analysts/news_analyst.py` ⚠️ 关键剥离点
- ✅ **保留**：`get_instrument_context_from_state`、asset_type/asset_label 逻辑、日期提示位置调整、`get_macro_indicators` 导入和挂载
- ❌ **删除导入**：`get_prediction_markets`（第 9 行）
- ❌ **删除挂载**：tools 列表中 `get_prediction_markets`（第 24 行）
- ❌ **删除 prompt 文本**：`and get_prediction_markets(topic, limit) for live market-implied probabilities of forward-looking events (e.g. 'Fed rate cut', 'recession 2026', geopolitical or sector events)`
- ⚠️ **必须同步改 prompt**：否则 LLM 会尝试调用不存在的工具。保留 `get_macro_indicators(...) to ground macro commentary in actual data from FRED (...)` 部分

#### `tradingagents/agents/analysts/sentiment_analyst.py`
- ✅ **完全采用上游版本**：结构化输出（`bind_structured` + `invoke_structured_or_freetext`）、`get_instrument_context_from_state`、prompt 重构

#### `tradingagents/agents/schemas.py`
- ✅ **完全采用上游版本**：`_coerce_optional_float`、TraderProposal/PortfolioDecision 的 field_validator、`SentimentBand` enum、`SentimentReport` model、`render_sentiment_report()`

### 3.6 Graph 层

#### `tradingagents/graph/setup.py` ⭐ 最高优先级
- ✅ **采用上游重构**：`build_analyst_execution_plan()`、`analyst_factories` dict dispatch、`DEBATE_PATH_MAP`/`RISK_ANALYSIS_PATH_MAP` 常量（修复 #1088 崩溃）
- 🔧 **手动接入 Evidence Steward**：在最后一个 analyst clear_node → Evidence Steward → Bull Researcher 处插入节点
- ⚠️ **不可修改** `DEBATE_PATH_MAP`/`RISK_ANALYSIS_PATH_MAP`（这是崩溃修复）

#### `tradingagents/graph/trading_graph.py` ⭐ 最高优先级
- ✅ **保留**：import 整理、类型注解现代化、`resolve_instrument_context()`、`_run_signature()`、`_coerce_max_retries()`、`propagate(asset_type)` 签名、`_get_provider_kwargs()`（temperature + max_retries）、`_fetch_returns()` 的 normalize_symbol、`save_reports()`、`write_report_tree` import
- 🔧 **身份解析分支（A 股优先）**：改造 `resolve_instrument_context()`，用 `is_a_share_ticker(ticker)` 分支：A 股调本地 `resolve_canonical_company_profile()`（tushare/akshare/yfinance 三级）并映射为 identity dict；非 A 股调上游 `resolve_instrument_identity()`。两者结果都传入 `build_instrument_context()`。详见第四节 4.1
- ✅ **保留 ToolNode**：market ToolNode 的 `get_verified_market_snapshot`、news ToolNode 的 `get_macro_indicators`
- ❌ **删除 ToolNode**：news ToolNode 的 `get_prediction_markets`（第 216 行）
- ❌ **删除 import**：`get_prediction_markets`（第 25 行）
- 🔧 **手动合并**：本地 Evidence Steward、A 股补充逻辑

#### `tradingagents/graph/analyst_execution.py`
- ✅ **完全采用上游版本**（135 行：AnalystSpec/AnalystExecutionPlan/build_analyst_execution_plan/AnalystWallTimeTracker）。与 setup.py、cli/main.py 紧耦合，必须用上游版本

#### `tradingagents/graph/checkpointer.py`
- ✅ **完全采用上游版本**：`thread_id(ticker, date, signature)` 图形态感知

### 3.7 配置与依赖

#### `tradingagents/default_config.py`
- ✅ **保留**：temperature/llm_max_retries、reasoning depth env 映射、`_coerce()` 加强（非法 boolean 报错）、gpt-5.5 默认、A 股 benchmark(.SS/.SZ)、macro_data 类别
- ❌ **删除**：`"prediction_markets": "polymarket"` 配置项（第 139 行）
- 🔧 **检查**：本地额外配置项（中文化相关）需保留

#### `pyproject.toml`
- ❌ **删除**：`[project.optional-dependencies]` 下的 `bedrock = ["langchain-aws>=1.5.0"]` 整段
- ✅ **保留**：其他依赖变更（yfinance>=1.4.1 等）
- ℹ️ fred/polymarket 无额外依赖（fred.py 纯实现）

#### `.env.example`
- ❌ **删除**：Bedrock 注释段（第 27 行）和 `#AWS_BEARER_TOKEN_BEDROCK=`（第 31 行）
- ✅ **保留**：FRED_API_KEY 等
- 🔧 **追加**：本地额外 key（tushare/akshare/tavily）

### 3.8 测试文件

| 文件 | 决策 |
|------|------|
| `tests/test_fred.py` | ✅ 引入 |
| `tests/test_bedrock_provider.py` | ❌ 不引入 |
| `tests/test_openai_compatible_provider.py` | ❌ 不引入 |
| `tests/test_polymarket.py` | ❌ 不引入 |
| 其他新增测试 | ✅ 引入（test_analyst_execution/test_symbol_utils/test_vendor_errors/test_structured_agents 等） |

---

## 四、本地独有功能保护与重新接入

这些是 fork 的核心价值，merge 时不会被覆盖（独立文件），但需确保集成后仍可用。

| # | 模块 | 功能 | 集成验证要点 |
|---|------|------|-------------|
| P1 | `agents/evidence_steward.py` | 证据质量评估 + Tavily 补充 | 在新版 `setup.py` 中重新接入 Evidence Steward 节点（analyst → steward → researcher） |
| P2 | `dataflows/china_data.py` | A 股数据源（tushare/akshare） | 在新版 `interface.py` 的 `VENDOR_METHODS` 中添加 china_data 条目 |
| P3 | `dataflows/tavily_news.py` | Tavily 新闻搜索 | 在 `VENDOR_METHODS['get_news']` 中添加 tavily 条目 |
| P4 | `dataflows/credibility.py` | 新闻源可信度评分 | 纯工具模块，验证调用处未被破坏 |
| P5 | `dataflows/consistency.py` | 跨源一致性检测 | 纯工具模块，同上 |
| P6 | CLI 中文本地化 | 中文界面 | port 到新版 cli/main.py |
| P7 | CLI JSON 配置 | 配置文件加载 | port 到新版 cli/main.py |
| P8 | 数据进度事件系统 | 数据调用进度 | port 到新版 cli/main.py |
| P9 | `dataflows/ticker_utils.py` | A 股 ticker 判定与符号转换（is_a_share_ticker/to_tushare_symbol 等） | 身份解析分支依赖此模块，必须保留 |

### 4.1 身份解析策略（A 股优先）

**背景**：上游 v0.3.0 新增 `resolve_instrument_identity()`（yfinance `.info`）+ `verified_market_snapshot()`；本地 Evidence Steward 用 `resolve_canonical_company_profile()`（tushare->akshare->yfinance 三级）。两者在"公司身份解析"上有重叠。

**决策**：以本地解析为准，但采用混合策略而非完全替换。

**原因**：本地 `resolve_canonical_company_profile()` 对非 A 股直接返回空 profile（evidence.py 第 136-137 行 `if not is_a_share_ticker(normalized): return profile`），完全替换会导致美股/加密/商品丢失身份解析。

**实现**（在 `trading_graph.py` 的 `resolve_instrument_context()` 内）：

```python
from tradingagents.dataflows.ticker_utils import is_a_share_ticker
from tradingagents.dataflows.evidence import resolve_canonical_company_profile

if is_a_share_ticker(ticker):
    profile = resolve_canonical_company_profile(ticker)
    identity = {
        "company_name": profile.get("name") or profile.get("full_name"),
        "industry": profile.get("industry"),
        "exchange": profile.get("exchange"),
    }
else:
    identity = resolve_instrument_identity(ticker)
return build_instrument_context(ticker, asset_type, identity)
```

**收益**：
- A 股用 tushare/akshare 正确解析（yfinance 对 A 股覆盖差且常返回英文名/错配）
- 所有 agent 从 propagate() 开始就锚定正确公司，而非等 Evidence Steward 事后纠正
- Evidence Steward 可从 state 复用已解析 profile，避免同一 ticker 查两次数据源
- 上游 `resolve_instrument_identity` / `build_instrument_context` / `verified_market_snapshot` 全部保留，非 A 股路径不受影响

**Evidence Steward 与上游 verified_market_snapshot 的关系**：两者职责不重叠，共存。上游 verified_market_snapshot 管"行情数值验证"（OHLCV+指标 ground truth，防 LLM 编造精确数值）；Evidence Steward 管"新闻证据质量门控"（credibility/consistency + Tavily 补充 + 三态门控）。一个防数值幻觉，一个防证据不足，互不干扰。

---

## 五、依赖完整性验证

剥离后必须确认无悬空引用。验证清单：

```
# 1. 确认无 polymarket 残留引用
grep -rn "polymarket\|prediction_market\|get_prediction_markets" tradingagents/ cli/ --include="*.py"

# 2. 确认无 bedrock 残留引用
grep -rn "bedrock\|BedrockClient\|AWS_BEARER_TOKEN_BEDROCK" tradingagents/ cli/ --include="*.py"

# 3. 确认无 openai_compatible 残留引用
grep -rn "openai_compatible\|LocalCompatibleChatOpenAI" tradingagents/ cli/ --include="*.py"

# 4. 确认无 kimi/groq/mistral/nvidia 残留引用
grep -rni "kimi\|groq\|mistral\|nvidia" tradingagents/ cli/ --include="*.py"

# 5. 确认 fred/macro 链路完整（应保留）
grep -rn "fred\|macro_data\|get_macro_indicators" tradingagents/ cli/ --include="*.py"

# 6. 确认 import 能成功
python -c "from tradingagents.graph.trading_graph import TradingAgentsGraph"
python -c "from cli.main import app"
```

预期：1-4 项无输出（或仅在注释/文档中），第 5 项有完整链路，第 6 项无 ImportError。

---

## 六、执行步骤

### 阶段 0：备份
```bash
cd /Users/david/codespace/TradingAgents
git checkout -b backup-pre-sync-20260709
git checkout main
git checkout -b fork-upstream-sync-20260709
```

### 阶段 1：执行 merge（不自动提交）
```bash
git merge upstream/main --no-commit --no-ff
```
预期：35 个文件冲突（见旧报告），冲突标记出现。

### 阶段 2：按优先级解决冲突

**第一梯队（🟢 低风险，直接取上游）**
- model_catalog.py（删不要的 provider 条目）
- symbol_utils.py、analyst_execution.py、checkpointer.py、schemas.py、sentiment_analyst.py（完全取上游）
- errors.py、fred.py、macro_data_tools.py、market_data_validation_tools.py、reporting.py（新文件直接采用）

**第二梯队（🟡 中风险，取上游 + 删条目）**
- openai_client.py（删 mistral/kimi/groq/nvidia/openai_compatible 条目 + LocalCompatibleChatOpenAI）
- factory.py（删 bedrock 分支）
- api_key_env.py（删 bedrock 条目）
- default_config.py（删 prediction_markets 配置）
- agent_utils.py（删 get_prediction_markets 导入）
- news_analyst.py（删 prediction_markets 导入 + 挂载 + prompt 文本）
- trading_graph.py（删 get_prediction_markets import + ToolNode 条目）
- interface.py（删 polymarket，保留 fred，添加 china_data/tavily）
- cli/utils.py（删 6 个 provider 表条目）
- pyproject.toml（删 bedrock 可选依赖）
- .env.example（删 bedrock 段）

**第三梯队（🔴 高风险，取上游 + 手动 port 本地功能）**
- setup.py（取上游重构 + 重新接入 Evidence Steward）
- cli/main.py（取上游 + port 中文化/JSON 配置/进度事件）

### 阶段 3：剥离清理
按"第五节依赖完整性验证"逐条 grep，确保无悬空引用。

### 阶段 4：测试
```bash
# 先跑单元测试
pytest -m "not integration" -x

# 重点测试
pytest tests/test_symbol_utils.py tests/test_vendor_errors.py tests/test_structured_agents.py tests/test_fred.py tests/test_analyst_execution.py
```

### 阶段 5：用户手动验证
- A 股 ticker（如 600036.SS, 000001.SZ）
- 美股 ticker（SPY, AAPL）
- 加密货币（BTC-USD）
- 商品/外汇（XAUUSD -> GC=F, EURUSD -> EURUSD=X）
- CLI 中文界面、JSON 配置加载

---

## 七、风险与注意事项

1. **news_analyst prompt 剥离**：最易遗漏。删 prediction_markets 工具必须同步删 prompt 文本，否则 LLM 调用不存在的工具报错。

2. **Evidence Steward 重新接入**：上游 setup.py 完全重写，本地 Evidence Steward 的接入位置需重新确定。建议在 `setup_graph()` 的 `plan.specs[-1].clear_node → Evidence Steward → Bull Researcher`。

3. **interface.py 本地 vendor 添加**：上游 `route_to_vendor()` 不再隐式 fallback，china_data/tavily 必须显式加入 VENDOR_METHODS，否则不会被调用。

4. **cli/main.py 本地功能 port**：这是工作量最大的文件。中文化/JSON 配置/进度事件需逐段 port 到上游新版结构。

5. **model_catalog openrouter**：openrouter 仍用 `_CUSTOM_ONLY`，不要误删这个辅助字典。

6. **可选数据源降级**：上游 v0.3.0 已移除 `OPTIONAL_CATEGORIES` 机制（代码中无此符号），改由 `route_to_vendor` 的 VendorError chain 实现 macro_data 失败时优雅降级。维护时无需查找 `OPTIONAL_CATEGORIES`。

7. **身份解析分支实现**：`resolve_instrument_context()` 改造时需 import 本地 `is_a_share_ticker`（ticker_utils.py）和 `resolve_canonical_company_profile`（evidence.py）。注意 profile dict 字段（name/full_name/industry/exchange）到上游 identity dict（company_name/sector/industry/exchange）的映射 -- 本地 profile 无 sector 字段只有 industry，映射时 sector 留空即可，`build_instrument_context` 对缺失字段有容错。Evidence Steward 的 A 股门控（解析不出 name 则 FAIL_STOP）保持不变。

---

*方案由 Proma Agent 基于上游 v0.3.0/v0.3.1 CHANGELOG + 实际代码 diff + 依赖分析生成，待用户审阅确认后执行*
