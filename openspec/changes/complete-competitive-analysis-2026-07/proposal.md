# Complete competitive-analysis improvements

## Why

`docs/competitive-analysis-2026-07.md` is the accepted scope. Partial slices
already exist, but the remaining A–I recommendations must become executable,
testable product capabilities rather than a pending roadmap.

## What changes

- Complete degradable A-share data capabilities and provider health handling.
- Make graph composition, PM constraints, research consensus, evidence, events,
  context handling, skills, and workbench views explicit and testable.
- Preserve the localhost-only, secret-free browser boundary and never persist
  private model reasoning.

## Success criteria

Every A–I item has an implementation or an explicit safe no-op boundary backed
by tests and current documentation; frontend consumers remain synchronized with
the backend contract.
