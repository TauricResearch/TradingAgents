# Handoff: SCAN-001-S02 Scan CLI Command

**Date:** 2026-05-21
**Agent:** pi (ses_7a6bb9 → ses_ebe2ae)
**Status:** S02 complete, in review. S03 ready to start.

---

## Epic: SCAN-001 — Technical Indicator Scan Engine

**td:** td-e7284c | **Brief:** `briefs/epic-technical-indicators-scan.md`

**Stories:**
| ID | Title | Status | Next |
|----|-------|--------|------|
| td-976bb3 | SCAN-001-S01: Indicator Library | ✅ COMPLETE | `td approve td-976bb3` (already done) |
| td-3faad7 | SCAN-001-S02: Scan CLI Command | ✅ COMPLETE, in review | **NEXT — approve S02** |
| td-7b2567 | SCAN-001-S03: Schema Additions | 🔲 Open | **Start after S02 approved** |
| td-904dd7 | Stock Card Component and View | 🔲 Open | After S03 |

**Dependency chain:**
```
S01 (done) → S02 (done, in review) → S03 (schema) → Stock Card View
```

---

## What was implemented (S02)

### Files created/modified
```
src/cli/commands/scan.ts     (400 lines — scan command)
src/cli/main.ts              (+1 line — register scan command)
```

### Commands
```bash
trading scan                     # scan SPY, QQQ, IWM (default)
trading scan AAPL               # scan single ticker
trading scan SPY QQQ            # scan multiple tickers
trading scan SPY --relax=rsi    # relax RSI gate
trading scan SPY --relax=rsi,macd,adx  # relax multiple gates
trading scan --json             # machine-readable JSON output
trading scan --plain            # suppress gum, plain ANSI
```

### Output format (plain)
```
=== SCAN: SPY ===
Date:        2026-04-08  |  Price:   497.19

✗ RSI < 30                     62.86
✗ Price ≤ Bollinger lower      474.1
✓ Price > MA20                 488.6
✗ ADX > 20                     7.15
✓ MACD histogram > 0           0.5830
✗ Volume confirmed             52.9M

✓ 150-day MA filter            475.0

Signal:      NO-BUY  (gate failed)
Exit:        clear (no triggers)
```

### Output format (JSON)
```json
{
  "scans": [{
    "ticker": "SPY",
    "date": "2026-04-08",
    "price": 497.19,
    "signal": "no_buy",
    "gates": [{ "name": "rsi", "pass": false, "value": 62.86, "relaxed": false }, ...],
    "gatesPassed": 2,
    "gatesTotal": 6,
    "ma150": { "passed": true, "value": 475.02 },
    "exitTriggers": []
  }]
}
```

### Gate symbols
- `✓` — gate passes
- `✗` — gate fails  
- `~` — gate relaxed (skipped)

### Error handling
- Insufficient data: `{ error, detail, hint }` JSON to stderr, exit code 1
- Unknown gate: `{ error, detail, hint }` JSON to stderr, exit code 1
- DB errors: caught and reported as JSON

### Git
- Branch: `scan-001-s02-scan-cli`
- Commit: `e871a7f` "SCAN-001-S02: scan CLI command — gate evaluation and exit triggers"
- Merge to main before starting S03

---

## S03: Schema Additions (next)

**What it needs:**
- Add `indicator_readings` table to schema.sql
- Add `scan_history` table to schema.sql
- `--no-store` flag to skip DB writes
- Scan CLI writes readings on each run (upsert by ticker, date)
- Scan CLI appends to `scan_history`
- `just check` must pass

**Schema (from brief):**
```sql
CREATE TABLE indicator_readings (
    ticker         TEXT NOT NULL,
    date           TEXT NOT NULL,
    price          REAL NOT NULL,
    rsi_14         REAL,
    bb_lower       REAL, bb_middle REAL, bb_upper REAL,
    ma_20          REAL, ma_150    REAL,
    adx_14         REAL,
    macd_line      REAL, macd_signal REAL, macd_histogram REAL,
    volume         INTEGER, volume_20avg REAL,
    created_at     TEXT DEFAULT (datetime('now')),
    PRIMARY KEY (ticker, date)
);

CREATE TABLE scan_history (
    id             INTEGER PRIMARY KEY AUTOINCREMENT,
    ticker         TEXT NOT NULL,
    date           TEXT NOT NULL,
    gates_passed   INTEGER NOT NULL,
    gates_total    INTEGER NOT NULL,
    signal         TEXT NOT NULL CHECK(signal IN ('buy','no_buy','sell')),
    exit_trigger   TEXT,
    created_at     TEXT DEFAULT (datetime('now'))
);
```

**Key constraints:**
- Uses `DatabaseFactory` for all DB writes
- Upsert for `indicator_readings` (same ticker+date = replace)
- Append for `scan_history`
- Default behavior: write to DB (backward compatible)
- `--no-store`: skip all DB writes

---

## Context for next session

1. **Merge S02 branch** to main before starting S03:
   ```
   git checkout main && git merge scan-001-s02-scan-cli && git push
   ```

2. **Approve S02:** `td approve td-3faad7`

3. **Start S03:** `td start td-7b2567`

4. **Stock card view** (td-904dd7) depends on S03 (schema). Don't start card view until S03 is complete.

---

## Known issues / decisions to preserve

- **gum-utils.ts unused** — `isGumAvailable()` exists in `src/cli/lib/gum-utils.ts` but not used by scan command yet. Keep for future gum integration.
- **150-bar minimum** — both `computeSnapshot` (indicators.ts) and scan CLI enforce this for MA150.
- **DB lock issue** — when using DatabaseFactory, don't leave connections open when spawning subprocesses.
- **Valid gate names** — `rsi`, `bollinger`, `ma20`, `adx`, `macd`, `volume` (order matches GATE_DEFINITIONS).
- **`Number.isNaN` required** — biome lint rule enforces this across codebase.

---

*Logged by: pi agent session ses_7a6bb9 (handoff to ses_ebe2ae)*
