# TradingAgents Local Web Handoff

Last updated: 2026-07-19

## Goal

Build a localhost-only web application that starts from the terminal, accepts a stock and analysis inputs, runs the real TradingAgents workflow, and visualizes agent outputs, debate turns, data/tool calls, run inputs, and final artifacts with clear role icons.

## Current phase

The user explicitly approved the corrected formal specification on 2026-07-18. The implementation plan is complete at `docs/superpowers/plans/2026-07-18-local-web-workbench-implementation.md`. Phases A, B, and C plus Stories D1, D2, D3, E1, E2, and E3 are implemented. The persistence boundary now covers redaction/canonical hashing, ticker-independent run directories, atomic snapshots, append-and-fsync events, restart history, traversal rejection, content-addressed artifacts, immutable retry-safe report revisions, and canonical report publication through a verified/fsynced temporary tree plus atomic rename. `RunStore` rejects `run.completed` unless `reports/complete_report.md` is already published. Observation separates stable logical turns from concrete LangGraph task invocations, durably correlates prompts, model attempts, logical tool requests, tool executions, retries, failures, and direct-call scopes, and rebuilds only committed or pending-apply tool requests after restart. All 13 roles now have exact code-audited state projections, Evidence Steward configuration drift protection, and observed role/tool/maintenance/input tasks that return checkpoint commit tokens. Vendor attempts preserve raw provider values where an adapter exposes them, keep adapter output distinct from normalized Agent input, record fallback lineage, and isolate news caching by analysis run. CLI, web, and programmatic execution converge on one successful-result-only `AnalysisRunner`. Checkpoint-enabled web runs are isolated by `run_id` and cannot open SQLite until the concrete durable fingerprint guard has persisted or strictly compared the actual resolved initial context, complete secret-free semantic configuration, runtime source/dependency identity, and schema versions. Checkpointed observed runs now use LangGraph `tasks`/`updates`/`checkpoints` streaming with synchronous durability, accept state only from a saver-verified checkpoint payload, reconcile the SQLite frontier against append-only candidates, resume with `None`, and promote state, tools, turns, roles, and report revisions only after the durable marker. Checkpoint-disabled observed runs use a values barrier and remain non-resumable. The local event broker now holds the shared per-run lock across append/fsync and publication, captures an atomic replay watermark, hands off to bounded loop-owned live queues without gaps or duplicates, isolates slow subscribers, emits only ephemeral keepalives, and closes terminal streams after their captured watermark without cancelling the underlying analysis. `SingleRunManager` now owns the one-active-run invariant, initializes all 13 role lifecycles, scopes and restores every process-global execution dependency, publishes immutable successful reports before terminal success, compensates open child lifecycles before cancel/failure/interruption, reconciles checkpoint state before startup interruption, supports new-run retry and same-run compatible resume, and writes terminal snapshot fields before terminal events. Resume authorization is frozen by a one-shot checkpoint guard before `run.resumed`; a missing checkpoint is rejected without mutating the stored run snapshot. The FastAPI boundary now exposes validated secret-free configuration, run commands/history, lazy run-scoped artifacts, persisted-envelope SSE with reconnect cursors, startup recovery, restrictive local asset headers, and SPA fallback that never captures unknown `/api/*` routes. Story E4 adds the loopback-only `tradingagents web` launcher, migrates CLI rendering onto the shared `AnalysisRunner` via a new `state_update_sink`, and preserves legacy root/`analyze` config, exit, and exception behavior. Story F1 scaffolds the React + TypeScript + Vite frontend building straight into the tracked `tradingagents/web/static` package, so `tradingagents web` serves a styled V2 three-column shell with no Node at runtime; the App shell only touches the real `/api/config` boundary and carries no fabricated role/event data. F2 typed contracts (`frontend/src/api/contracts.ts` + `frontend/src/state/model.ts`) are landed and programmatically audited against the backend event schema and the 13-role registry with zero drift. Story F2 implements the shared live/history event reducer (`runReducer.ts`), typed API client (`client.ts`), SSE `eventSource.ts`, the `useRunStream` hook, and the `WorkbenchStore` context; the core batch-replay-vs-one-by-one-live deep-equal invariant is covered by a reducer unit test, and `run.cancel_requested` was added to the typed event union (it was missing from the F2 type design). Story F3 adds the left-sidebar controls (ticker/date/analysts/depth/provider/models/language/checkpoint + key status + start/cancel) and the run history list, wired into the three-column V2 layout; provider->model linkage lives in the `useConfig` hook (switching provider resets quick/deep to that provider's first option), `/api/config` is the single authority, and the browser never receives secret values. Story G1 adds the 13-role workflow map in the center column: 3-row grid with custom inline SVG icons (ported verbatim from the V2 mockup), Chinese labels, per-role status (pending/running+round/completed/failed/cancelled/interrupted/skipped/not_reached), and a progress note; the map renders all 13 pending placeholders even before any run starts (design A) so the workflow structure is always visible.

## Confirmed constraints

- Localhost only; no hosted site or production deployment.
- The web UI must run the real TradingAgents graph rather than a simulated demo.
- Debate turns, agent output, tool/data calls, inputs, progress, and final reports must be inspectable.
- The UI should be compact, clear, visually polished, and use distinct role icons.
- Decisions should start from the real objective, constraints, observable runtime events, and failure conditions.
- Only one analysis may be active at a time in the first local version.
- Completed and failed runs remain available for history and report review.
- API keys remain in `.env` or the local config file. The browser only receives configured/missing status and never receives or stores secret values.
- The application will use a full local website architecture: FastAPI backend plus React, TypeScript, and Vite frontend.
- The richer frontend is intentional because debate visualization requires interactive filtering, expandable tool/data calls, live state, and historical replay.
- The website remains localhost-only and is not deployed or publicly hosted.
- The approved runtime boundary is React UI -> FastAPI REST/SSE -> single-run manager -> background worker -> shared AnalysisRunner -> real LangGraph.
- Every run has an independent `run_id` directory with a non-secret input/status snapshot, append-only event log, and report artifacts.
- The built React assets are served by FastAPI for normal use; Node is only needed for frontend development/building.
- The approved event model uses an append-only, versioned event envelope with monotonic sequence numbers for SSE delivery and historical replay.
- Graph node updates are the authority for actor identity; callbacks add tool lifecycle details; debate cards use per-turn current responses rather than cumulative history.
- Large tool results are stored as referenced run data files, while SSE carries a safe summary and reference.
- UI feedback: replace placeholder glyphs with 13 distinctive custom role icons that share one coherent visual language.
- UI feedback: every role needs an auditable input view showing the exact evidence, upstream reports, tool results, and instruction context it actually received, including source and time-range metadata. Examples include company overview, balance sheet, income statement, cash flow, market data, and technical indicators.
- The input view must help the user verify data accuracy; a tool-call list alone is insufficient.
- Per-role input capture is now designed as two layers: a node-entry whitelist snapshot for the actual state fields each role reads, plus an LLM-start callback snapshot for the fully formatted prompt/messages actually sent to the model.
- Shared financial tables and large tool results are content-addressed artifacts; role events reference their hashes instead of duplicating data.
- Sentiment Analyst direct news/social prefetches and Evidence Steward advisor/enrichment calls require explicit observer injection because the current graph callbacks do not see them.
- Aggregate role-card status is separate from immutable logical turn lifecycles, so repeated Bull/Bear and risk rounds remain auditable.
- Resume is restricted to interrupted runs and requires a canonical full-config/runtime fingerprint plus event-log/checkpoint correlation reconciliation.
- News-result caching is run-scoped; same-run cache hits reference the original raw and normalized artifacts.
- `AnalysisResult` represents success only. Web cancellation/failure state stays in the run store, while programmatic tuple and exception behavior remain compatible.
- Checkpoint-enabled web runs use synchronous durability and never treat ordinary stream updates as durable commits.
- Every observed graph task carries a reserved checkpoint commit token; recovery classifies JSONL tails as committed, pending-apply, or abandoned without rewriting history.
- Resume fingerprints include Python runtime identity and the installed transitive dependency closure with package metadata digests.
- The V2 visual mockup exposes all 13 roles separately, uses custom inline SVG icons, and adds a role audit inspector with data fields, upstream inputs, prompt view, and raw vendor values.
- V2 browser QA passed on 2026-07-18: Playwright confirmed all 13 role nodes are present, role selection switches the input composition, raw-value and main inspector tabs work, and closed tool details expand correctly.
- The only initial browser console error was a missing favicon; the mockup now uses an empty data favicon and reloads without console errors.
- The user approved the V2 visual layout, custom 13-role icon system, and per-role input audit interaction on 2026-07-18.

## Repository evidence gathered

- `TradingAgentsGraph` already accepts callback handlers and exposes the LangGraph workflow.
- The CLI already streams graph chunks, classifies AI/tool messages, tracks agent status, records data-progress events, and writes reports.
- Final state already contains analyst reports, investment debate state, trader plan, risk debate state, and final trade decision.
- The current data-progress sink is process-global, so concurrent web runs need isolation or an explicit single-active-run rule.
- The project is currently Python-only; no frontend framework or web server dependency is installed.
- The CLI already extracts agent messages, tool arguments, tool results, debate state, risk state, and data-vendor progress from the real graph stream.
- The synchronous LangGraph stream must run in a worker thread so the web server event loop stays responsive.
- A shared `AnalysisRunner`/event adapter should serve both CLI and Web paths to avoid creating a third execution implementation.
- The authoritative interpreter is `/Users/david/miniconda3/bin/python` (Python 3.13.5) from a login shell; it provides pytest and LangGraph.
- The focused pre-change legacy baseline passed with 41 tests. After Story A1, the expanded legacy matrix passed with 43 tests and the A1/relevant compatibility matrix passed with 97 tests.
- Typer's prior single-command collapse made `tradingagents [OPTIONS]` work while README-documented `tradingagents analyze [OPTIONS]` failed. The callback/group boundary now supports both, so adding `web` will not silently break the legacy root invocation.
- Configured selections previously omitted `asset_type` and used only A-share ticker normalization. They now use the canonical CLI normalizer and explicitly classify stock versus crypto.
- The installed graph runtime is exactly at the approved floors: LangGraph 1.1.10, langgraph-checkpoint 4.0.3, and langgraph-checkpoint-sqlite 3.0.3. The temporary SQLite capability probe passes all required checks.
- The approved `[web]` extra is installed in the authoritative environment. Full preflight currently passes with FastAPI 0.139.2, Uvicorn 0.51.0, RFC 8785 0.1.4, and the exact approved LangGraph floors.
- Phase A focused web-contract tests passed with 36 tests. The complete repository suite then passed with 737 tests and 68 subtests; 18 existing model-catalog warnings remain.
- B1 focused redaction, canonicalization, provider-registry, checkpoint, and analyst-graph tests pass with 57 tests. The preflight suite still passes after installing the complete web extra.
- `mimo` was selectable but absent from the canonical provider credential registry; it now resolves to `MIMO_API_KEY`, matching the existing provider client and local config behavior.
- B2 RunStore/event/artifact coverage passes together with the event/redaction/canonical contracts: 44 focused tests.
- All web tests through Phase B pass with 67 tests. The complete repository suite after the web dependency install and AgentState/report changes passes with 769 tests and 68 subtests; the same 18 model-catalog warnings remain.
- Story C1 callback capture uses the installed LangChain callback signatures and public LangGraph task identity boundary. One logical turn can now truthfully carry distinct role, tool, and role-reentry task IDs without losing its debate-turn identity.
- C1 tests cover callback-manager assertion propagation, AI message usage metadata, callback-order-independent tool joins, restart filtering of candidate versus pending-apply requests, direct-call correlation, retries, interruption/resume, and durable-before-forget terminal callbacks.
- All web tests through Story C1 pass with 81 tests. Python compilation and `git diff --check` pass. Ruff is declared only in the optional development dependencies and is not installed in the authoritative interpreter, so no Ruff result is claimed.
- The C2 role projection matrix was audited against each role's real state access. Instrument identity fallback fields are captured only when the helper would read them; debate and risk nested dictionaries exclude fields the specific role does not read; every role references the immutable effective-config artifact instead of duplicating it.
- `EvidenceConfigSnapshotV1` freezes the approved exact configuration whitelist plus every normalized `tavily_*` key, redacts credentials, normalizes endpoint identity, and fails before Evidence Steward evaluation when actual process configuration drifts from the run's frozen configuration.
- Evidence Steward's `canonical_company_profile`, `evidence_status`, and `evidence_report` outputs were previously undeclared and therefore silently discarded by LangGraph. They are now declared application channels, initialized in input state, included in business hashing, and covered by checkpoint-candidate tests.
- Observed graph tasks use the public `Runtime.execution_info.task_id` and `metadata.langgraph_step`. A real LangGraph role -> ToolNode -> role re-entry test proves one stable turn with three distinct task IDs and tool joins by model `tool_call_id`; initial input and every maintenance writer receive their own commit token.
- Observation persistence failures are now distinct from provider/model failures and cannot be swallowed by structured-output fallback.
- All web tests through Story C2 pass with 109 tests. The affected compatibility matrix passes with 152 tests. The complete repository suite passes with 812 tests and 68 subtests; the same 18 existing model-catalog warnings remain. Python compilation and `git diff --check` pass; Ruff remains unavailable in the authoritative interpreter.
- Story C3 uses one provenance model for routed tools and direct data calls. Every vendor attempt receives a stable `vendor_call_id`, duration, safe terminal status, period/unit/currency fields, fallback lineage, adapter-output artifact, normalized-result artifact, and true raw artifacts when the provider seam exposes them. Missing raw capture is explicitly recorded as `unavailable`; normalized adapter output is never mislabeled as raw.
- Raw capture seams now cover yfinance company profiles, OHLCV, technical indicators, statements, and news; Alpha Vantage responses; Tushare/AKShare profiles, OHLCV, fundamentals, and statements; Tavily news and enrichment; FRED responses; StockTwits JSON; and Reddit RSS/JSON. Artifact persistence redacts before writing, and provider exceptions persist only a safe type/generic message rather than credential-bearing exception text.
- Sentiment's direct news, StockTwits, and Reddit prefetches, plus Evidence Advisor, consistency clustering, and Tavily enrichment, now inherit the active observer. Dynamically created Evidence LLMs receive the same callback, so these calls remain attached to the correct role turn and graph task.
- News caching now requires an explicit run-owned scope, is destroyed on scope exit, cannot leak across runs, and treats provenance-free entries as misses in observed execution. Cache-hit events reference the original ordered vendor attempts and artifacts instead of synthesizing new provider calls.
- C3's focused verification passes with 65 tests; additional China/FRED/provider coverage passes with 41 tests; C3 Ruff checks pass. The complete repository suite after C3 passes with 821 tests and 68 subtests in 183.74 seconds; the same 18 existing model-catalog warnings remain.
- Story D1 moves pending-memory resolution, past context and identity resolution, initial-state construction, graph invocation/streaming, state logging, decision-memory writes, checkpoint lifecycle, and signal processing behind one consumer-neutral `AnalysisRunner`. `propagate()` is now only the legacy `(final_state, final_signal)` adapter, while `run_analysis()` returns `AnalysisResult` and accepts cooperative cancellation plus an observed graph context.
- Completion preserves the legacy order `curr_state -> state log -> decision memory -> checkpoint clear -> signal`; original provider/graph exceptions retain identity, reports remain explicit/outside the runner, and cancellation is checked only before work or after a completed graph stream boundary. Once the completion tail begins, success wins the cancellation race.
- Checkpoint ownership is fail-safe across context enter, graph compile, checkpoint inspection, graph execution, close, and plain-graph restoration. A cleanup failure cannot mask the active analysis error, and no partially entered saver remains attached.
- D1's focused compatibility matrix passes with 107 tests; its dedicated runner matrix covers 10 execution/cancellation/checkpoint cases. The complete repository suite after D1 passes with 831 tests and 68 subtests in 189.97 seconds; the same 18 existing model-catalog warnings remain.
- Story D2 defines the exact `ResumeFingerprintV1` document and a production-only builder that derives source, Python, installed transitive dependency, serializer/projection, event, `AgentState`, complete effective-config, request, and resolved initial-context identity from the actual runtime. Test-only runtime injection is confined to an underscore-private builder.
- Effective configuration identity removes credential leaves and credential-only shells recursively, excludes exactly the four approved root location keys, normalizes backend endpoint identity, and retains all other current/future semantic keys. Runner and fingerprint comparisons use this same secret-free projection, so API-key rotation or omission does not become false semantic drift while model/vendor/policy changes still fail closed.
- Runtime source hashing covers every semantic Python path and byte under `tradingagents`, detects whole-manifest races, and excludes only explicit root tests/fixtures/frontend plus `__pycache__`. Dependency closure follows installed `Requires-Dist` recursively regardless of markers, hashes exact `.dist-info`/`.egg-info` metadata and sanitized direct-URL identity, and disables resume on invalid requirements, empty/unverifiable records, invalid direct URLs, or failed checkpoint capabilities. The installed environment currently fingerprints 138 distributions with no issues.
- `FingerprintCheckpointGuard` is the only Runner-accepted web checkpoint guard. For a fresh run it atomically stores the canonical document/digest in `run.json` before initial observation or SQLite; for resume it requires a stored fingerprint, rechecks canonical digest integrity, and returns only safe mismatch categories. Missing, corrupt, non-resumable, or semantically incompatible fingerprints fail before synthetic input observation and graph execution. Runner rejects generic/no-op callbacks even if they manufacture a field-valid authorization token.
- Web checkpoint helpers namespace thread IDs by durable `run_id` without changing legacy golden IDs. Full access returns the raw latest and parent `CheckpointTuple` plus untouched pending writes, and exact run-scoped cleanup cannot affect sibling web runs or legacy checkpoints. Observation `run_id` is automatically bound to the same namespace and mismatches fail before work.
- D2 focused fingerprint/runner/checkpoint/preflight verification passes with 79 tests; the broader web/CLI compatibility matrix passes with 210 tests. D2 Ruff checks, Python compilation, and `git diff --check` pass. The complete repository suite after D2 passes with 887 tests and 68 subtests in 184.71 seconds; the same 18 existing model-catalog warnings remain.
- Story D3 introduces strict `ObservationCommitV1` reconciliation across the latest/parent SQLite checkpoints, pending writes, and append-only JSONL events. Durable map additions become committed, valid saver pending writes remain `pending_apply`, and unmatched candidates or started-only attempts become `abandoned`; a later retry with the same LangGraph task ID is accepted only after the prior attempt has an explicit abandonment boundary.
- Tokenless checkpoints are accepted only as proven barriers: business-state hashes must match, declared application channels cannot be updated, and the parent writes that produced the checkpoint cannot touch application state. Existing marker contents, candidate artifact bytes/digests, serializer/projection/schema identities, checkpoint routes, event sequence, and live checkpoint ID are checked fail-closed before appending.
- Checkpointed observed web runs force `stream_mode=["tasks", "updates", "checkpoints"]`, `durability="sync"`, and `version="v1"`. Every streamed checkpoint is reread from the same saver and compared with the compiled graph's exact `get_state(...).next`; ordinary updates never become accepted state. Resume passes `None`, does not recreate synthetic input, preserves durable pending work, and can finish from a terminal saved state without re-running the graph.
- Marker application is append-only and drift-guarded. State/report/tool/turn/aggregate-role promotion occurs only after `graph.checkpoint_committed` (or `graph.step_applied` for checkpoint-disabled execution), is retry-safe after partial promotion, and keeps multi-tool commits in the ordered logical `tool_call_id` list from the token. Repeated abandoned requests bind cancellation/commit terminals by event sequence, so an old terminal cannot close a later reuse of the same logical tool ID. Identical partial-report content reuses its immutable revision, and content-addressed report artifacts remain readable even when multiple report kinds share the same bytes.
- Bootstrap recovery handles both pre-step and step `-1` crash windows. A fresh retry with no SQLite checkpoint strictly revalidates and reuses the one synthetic input candidate/token/artifact instead of appending a duplicate. A resume from LangGraph step `-1` preserves `_resume_state` and continues with `stream(None)` without prematurely abandoning the not-yet-applied input candidate; step `0` remains the first input commit barrier.
- D3's pure frontier matrix covers commit-map diff/removal/change, candidate/token/artifact validation, pending-apply, abandonment/retry, barrier-only checkpoints, marker idempotency/conflicts, and repeated logical-tool lifecycle sequences. The real crash matrix uses `StateGraph + SqliteSaver + DurableRunObserver` to verify fresh sync streaming, `None` resume without first-node replay, both bootstrap windows, checkpoint-disabled barriers, cancellation after barriers, append-only DB-ahead recovery, role/report completion ordering, and ordered multi-tool commit promotion. The focused D3 matrix passes with 38 tests; the broader web/checkpoint compatibility matrix passes with 226 tests. Final D3 Ruff checks and `git diff --check` pass. The complete repository suite passes with 920 tests and 68 subtests in 180.39 seconds; the same 18 existing model-catalog warnings remain. An independent final read-only audit reproduced and verified all three repaired P1 crash boundaries and found no remaining P0/P1 issues.
- Story E1 adds an atomic `EventBroker` whose publication path persists and fsyncs before scheduling any live delivery under the same `RunStore` per-run lock used by subscription. Subscription captures a disk watermark and registers while that lock is held, replays only `(after, watermark]`, then accepts loop-thread events above the watermark. Each subscriber has an independently bounded deque and async condition; overflow clears and closes only that subscriber as `slow_consumer`, while reconnect can recover every event from disk. Browser close unregisters the subscriber without changing run state, keepalives remain in memory, concurrent worker publication preserves persisted sequence order, and terminal subscriptions discard post-watermark live events before closing. E1 plus RunStore regression passes with 16 tests; Ruff, Python compilation, and `git diff --check` pass.
- Story E2 adds `SingleRunManager` with an atomic running/terminalizing ownership phase so a second submission always receives a deterministic conflict and a persisted cancellation cannot lose to success terminalization. The manager creates the run, effective-config artifact, `run.started`, and all 13 initial aggregate-role events before worker execution; selected/fixed roles start pending and unselected roles are explicitly skipped. Success is accepted only after every reachable role is terminal and the canonical report tree is durably published. Cancel, failure, and startup interruption close model, vendor, tool-execution, logical-tool, turn, and aggregate-role lifecycles in parent-safe order without rewriting history.
- E2 startup recovery first reconciles the durable SQLite/event frontier, promotes committed work, abandons uncommitted no-checkpoint tails, closes dangling tools, and only then emits `run.interrupted`. Retry creates a new run; resume reuses the interrupted run only after a one-shot frozen fingerprint/checkpoint preauthorization and a dry-run frontier check. The exact pre-resume event sequence is recorded on `turn.resumed`, and terminal snapshot status/report/error fields are persisted before the corresponding terminal event becomes visible to SSE consumers.
- E2's latest focused manager/lifecycle/fingerprint/runner matrix passes with 88 tests, and the complete web suite passes with 233 tests. The complete repository suite passes with 946 tests and 68 subtests in 223.25 seconds; the same 18 existing model-catalog warnings remain. Python compilation and `git diff --check` pass. The independent runtime audit verified the final checkpoint-missing repair and found no remaining P0/P1 issues; the repair includes a zero-snapshot-mutation regression. Ruff had passed before the final review fixes but could not be rerun for the last delta because the environment rejected the required cache access; no workaround or unverified Ruff claim is used.
- Story E3 adds a FastAPI application factory whose lifespan reconciles orphaned runs before serving. `POST /api/runs` validates ticker syntax and canonical asset identity, non-future date, registry-ordered analyst selection, research depth, output language, checkpoint capability, provider readiness, and provider/mode model choice before calling `SingleRunManager`. Provider readiness is shared with `/api/config`; Azure requires key, endpoint, and API version, while Tushare data status accepts either supported credential name. Responses expose only configuration booleans and safe run snapshots, and resume conflict diagnostics use a fixed audited allowlist rather than returning exception text or arbitrary field names.
- E3 serves newest-first history plus cancel/retry/resume commands with stable status codes; artifact listing reads only persisted `artifact.written` metadata and bodies are integrity-checked lazily inside the selected run. SSE honors matching `after`/`Last-Event-ID`, emits the complete persisted envelope and ephemeral keepalives, closes after terminal replay or a live terminal event, and unregisters browser disconnects without cancelling analysis. A discovered future-cursor boundary is fixed in `EventBroker`: live delivery starts after `max(watermark, after)`, so an ahead browser cursor cannot receive lower sequences. Static assets receive restrictive CSP/referrer/frame/content-type headers, client routes fall back to the SPA, and unknown `/api/*` paths remain JSON 404s.
- E3's latest API/SSE/broker focused matrix passes with 57 tests, and the complete web suite passes with 282 tests. The complete repository suite passes with 995 tests and 68 subtests in 178.81 seconds; its 19 warnings are the 18 existing model-catalog warnings plus FastAPI TestClient's upstream Starlette deprecation notice. Python compilation and `git diff --check` pass. An independent final runtime audit verified both repaired P1 boundaries and found no remaining P0/P1 issues.
- Story E4 adds the loopback-only `tradingagents web` launcher and migrates terminal rendering onto the shared `AnalysisRunner`. `launch_web()` preflights, composes the FastAPI app with `checkpoint_available` from the probe, prints the loopback URL, optionally opens the browser, and serves Uvicorn on `127.0.0.1` with no host override. The `web` Typer command lazily imports the launcher so the base CLI and package never require the web extra; a missing extra or `WebRuntimeError` prints the exact `INSTALL_COMMAND` before a nonzero exit. `cli/run_observer.py:CliRunObserver` translates the runner's raw state-update chunks (messages, tool calls, investment/risk debate state, trader plan, role-status transitions) into the existing `MessageBuffer`/`update_display` pipeline. `cli/main.py:run_analysis` no longer opens a direct `graph.graph.stream()` loop; it builds an `AnalysisRequest` from the configured selections and calls `graph.run_analysis(request, callbacks=[stats_handler], state_update_sink=run_observer)`, so CLI, web, and programmatic callers share one execution path. `AnalysisRunner.run()` gains a `state_update_sink` that receives every `updates`/`values` stream chunk without spawning a second graph execution; checkpoint-enabled web runs keep their `tasks`/`checkpoints` stream unchanged. Legacy root and `analyze` commands preserve their config/checkpoint flags, success exit, and original exception propagation. E4's focused web-CLI and CLI/runner compatibility matrix passes with 44 tests; Python compilation and `git diff --check` pass.
- Story F1 scaffolds the approved React + TypeScript + Vite workbench. `frontend/vite.config.ts` builds straight into `tradingagents/web/static` with `base="/"` and hashed assets under `assets/`; `pyproject.toml` package-data adds `web/static/*` and `web/static/assets/*` so a clean wheel serves the SPA without Node. `App.tsx` renders the V2 three-column shell from ported visual tokens (12 colors + radius + fonts in `tokens.css`, 58px topbar + 276/1fr/350px layout in `workbench.css`) and its only stateful touch is a real `/api/config` health check - no mock role/event/debate data lands yet (those are F2/G1). `tests/web/test_package_assets.py` gates on a built `index.html` plus self-consistent hashed assets and prints the exact npm build command on failure. `tradingagents web` was verified end to end: shell served at `/`, `/api/config` returns 15 providers + DeepSeek default + checkpoint_available, SPA fallback returns 200 text/html, unknown `/api/*` returns JSON 404, and CSP/referrer/X-Frame/X-Content-Type security headers are present. F1 typecheck, vitest unit, and package-asset tests pass; `npm install` + `npm run build` are the only Node steps and their committed output is drift-checked. `tests/web/test_web_cli.py` now strips ANSI before substring assertions so the launcher contracts hold under `FORCE_COLOR` (Rich was splitting `--port`/install-command across styled runs). F2 typed contracts (`frontend/src/api/contracts.ts`, 627 lines; `frontend/src/state/model.ts`, 386 lines) are landed: a discriminated `EventPayloadByType` union covers every backend event type from `required_payload_fields`, the 13-role registry matches `roles.py` actor-for-actor with zero missing/extra, and no `any` is used (opaque blobs are `Record<string, unknown>` with inline justification); a programmatic audit script confirmed alignment.
- Story F2 lands the shared live/history reducer and the typed API/SSE client. `frontend/src/state/runReducer.ts` is a pure deterministic reducer over every backend event family (run/role/turn/model/tool/data/input/artifact/report/graph/stats): it seeds all 13 roles from `run.started` with the selected/unselected split, dedupes by `sequence` (an event with `sequence <= latest_sequence` is a no-op), ignores unknown event types per spec 9.5, and converts still-pending roles to `not_reached` on terminal failure/cancel (running roles to `interrupted`). `selectors.ts` derives `roleList` (registry order), `turnTimeline` (insertion order, team filter), `currentRunStatus`, `reportsByKind`, and `isTerminal`. `frontend/src/api/client.ts` wraps every `/api/*` endpoint with a typed `request<T>` that parses the backend `{detail:{code,message,fields,active_run_id?}}` envelope into `ApiError` and guards `run_id` before path interpolation. `frontend/src/api/eventSource.ts` consumes the SSE stream over the browser `EventSource` API, reconstructs `PersistedEventDTO` from `id`/`event`/`data`, relies on `Last-Event-ID` for reconnect, and closes after terminal events. `frontend/src/hooks/useRunStream.ts` binds `useReducer` to `getRun()` + `openRunStream()`: a `snapshot` action seeds replay, live `event` actions fold on top, and unmount/run_id change closes the subscription. `frontend/src/state/WorkbenchStore.tsx` provides per-run context isolation. `run.cancel_requested` was added to `EventPayloadByType` (it was missing from the F2 type design - the backend `events.py` `startswith('run.')` branch handles it but the literal union omitted it). `runReducer.test.ts` proves the core invariant - batch replay and one-by-one live reduction of a 14-event slice yield deep-equal state - plus sequence dedupe, unknown-type tolerance, 13-role init split, repeated Bull rounds keeping distinct turn_ids on one role card, and terminal failure converting pending roles to `not_reached`. Typecheck, 7 vitest tests, and `vite build` pass.
- Story F3 lands controls + run history + the three-column workbench layout. `frontend/src/hooks/useConfig.ts` owns all selection state and the provider->model linkage (confirmed design (A): on `setLlmProvider(v)` the hook resets `quick_think_llm`/`deep_think_llm` to the new provider's first quick/deep option); `buildRequest()` validates ticker-non-empty, at-least-one-analyst, provider-configured, and non-empty quick/deep, returning `RunCreateRequestDTO | null` plus a human-readable `validationError`. `frontend/src/hooks/useRunHistory.ts` wraps `listRuns()` + `refresh()`. `frontend/src/components/controls/Controls.tsx` is a pure renderer: start button disabled on `validationError` or active run (`stream.status` in live/replaying/loading), cancel button while running, key-status line shows configured/missing only (never the value). `frontend/src/components/history/RunHistory.tsx` renders the newest-first list with colored status badges (completed=green, failed=red, running=gold, etc.), click -> `selectRun`, active-item highlight, and an empty placeholder. `frontend/src/components/layout/WorkbenchLayout.tsx` wires F2 stream + F3 controls/history into the V2 three-column shell; the center shows a run status strip (ticker + status + latest_sequence) when a run is selected, else a placeholder. `App.tsx` wraps `WorkbenchLayout` in `WorkbenchProvider`. `frontend/src/test/setup.ts` adds `afterEach(cleanup)` so `render` does not leak between tests. Tests: Controls (5: render from config, provider switch resets models, start disabled on unconfigured provider / no analysts, createRun+selectRun wiring) + RunHistory (3: render fixture, click->selectRun, empty placeholder). 15 frontend tests pass; typecheck + `vite build` pass. `tradingagents web` verified: shell served at `/`, `/api/config` returns 15 providers + DeepSeek default, `/api/runs` returns history, hashed assets resolve.
- Story G1 lands the 13-role workflow map. `frontend/src/components/icons/roleIconPaths.tsx` ports all 13 SVG path bundles verbatim from the approved V2 mockup (chart-bars, speech-pulse, newspaper, institution-columns, verified-magnifier, rising-horn, falling-paw, scales, opposing-arrows, lightning, centered-crosshair, shield, portfolio-compass); `RoleIcon.tsx` renders them stroke-based with `currentColor` so icons inherit team color (green/red for bull/bear, cyan for risk, blue default). `frontend/src/domain/roles.ts` defines `STAGES` (6: analysts/evidence/research/trading/risk/portfolio), `ROLE_LAYOUT` (13 entries in the 3-row V2 grid with `wide` flags for evidence/manager.research/trader/portfolio), `ROWS`, `ROLE_LABELS_ZH`, and `stageColorClass(actor_id)` mapping bull/bear/risk. `RoleCardView.tsx` renders one node: icon + Chinese label + a status line covering all 8 RoleStatus values (pending='待运行', running='● 运行中 · 第 N 轮', completed='✓ 已完成', failed='✗ 失败', cancelled='已取消', interrupted='已中断', skipped='未选择', not_reached='未到达') with .node.done/.live/.bull/.bear/.risk/.wide classes. `WorkflowMap.tsx` consumes `useWorkbenchStore`, renders 3 `.flow-row` grids, and - per confirmed design (A) - renders all 13 pending placeholders when `stream.state === null` so the workflow structure is always visible; a progress note shows `N / 13 已完成`. `workbench.css` ports the V2 `.workflow`/`.flow-row`/`.node*` styles. `WorkbenchLayout` now renders `WorkflowMap` in the center column (under the run status strip when a run is active). Tests: 6 (13-cardinality + Chinese labels, icon_id uniqueness against `ROLE_ICON_PATHS`, completed/running+round status rendering, skipped truthfulness - a skipped node shows '未选择' not '待运行', progress count, click->onRoleSelected). 21 frontend tests pass; typecheck + `vite build` pass.

## Pending decisions

None. Material product and architecture decisions are approved. Pause only if implementation evidence exposes a new choice that would change the agreed scope or truthfulness of the audit trail.

## Next steps

1. G2: debate/verdict timeline - one item per turn_id with team/role filters, current response (not cumulative history), manager/portfolio verdicts highlighted, candidate/abandoned output separated from committed (frontier derivation).
2. F3: controls (ticker/date/analysts/depth/provider/models/language/checkpoint), key status, and run history selection, all driven by `/api/config` as authority.
3. Then Phase G (13-role workflow + custom SVG icons, debate/verdict timeline, role-input/data/tool/config/artifact inspector) and Phase H (Playwright e2e, package/CI/docs gates, one real minimum-depth smoke test).
2. Then Phase G (13-role workflow + custom icons, debate/verdict timeline, role-input/data/tool/config/artifact inspector) and Phase H (Playwright e2e, package/CI/docs gates, one real minimum-depth smoke test).

## Notes

- The standard brainstorming visual-companion launcher is missing from the installed skill package. A local static mockup server will be used as a fallback for visual comparisons.
