services:
  handelsagenten:
    build: .
    env_file:
      - .env
    volumes:
      - tradingagents_data:/home/appuser/.handelsagenten
    tty: true
    stdin_open: true

  ollama:
    image: ollama/ollama:latest
    volumes:
      - ollama_data:/root/.ollama
    profiles:
      - ollama

  handelsagenten-ollama:
    build: .
    env_file:
      - .env
    environment:
      - HANDELSAGENTEN_LLM_PROVIDER=ollama
      - OLLAMA_BASE_URL=http://ollama:11434/v1
    volumes:
      - tradingagents_data:/home/appuser/.handelsagenten
    depends_on:
      - ollama
    tty: true
    stdin_open: true
    profiles:
      - ollama

volumes:
  tradingagents_data:
  ollama_data:
