apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tradingagents-pvc
  namespace: tradingagents
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
